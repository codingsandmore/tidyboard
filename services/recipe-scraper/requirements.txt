# Runtime dependencies for Docker image builds.
# These are the direct dependencies; pip resolves transitive deps.
# To regenerate a fully-pinned lockfile for a specific Python version:
#   uv pip compile pyproject.toml -o requirements.txt --python-version 3.13
#
# NOTE: Exact pins below are for Python 3.13-slim (the Docker base image).
# Running on a different Python version (e.g. 3.14 in dev) may require
# letting pip resolve freely: pip install recipe-scrapers beautifulsoup4 ...

recipe-scrapers>=14.55.0
beautifulsoup4>=4.12.0
httpx>=0.27.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
structlog>=24.1.0
